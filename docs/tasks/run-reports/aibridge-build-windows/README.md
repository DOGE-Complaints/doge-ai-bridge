# AI Bridge build windows

Производные артефакты CLI `--write-build-window` для профиля `aibridge`.

Не править вручную как SSOT очереди — источник: active `pkg-*.yaml`.

```bash
python3 docs/methodology/Zeya888-builder-queue/cli/builder_resolve_queue.py --project aibridge --write-build-window --window-flat-start 1 --window-flat-end K
```
